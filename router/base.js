const router = require('express').Router()
const db = require('../libs/db')

// router.get('/', (req, res) => {
//   res.send('Hello World')
// })

// router.get('/', (req, res) => {
//    db.query('sql query', parameters)
//    .then(result => res.send(result))
//    .catch(error => next(error))
// })

module.exports = router
